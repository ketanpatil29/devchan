import React from "react";

const Login = () => {
    return (
        <section>
            <div>
                <button>Login</button>
            </div>
        </section>
    );
}

export default Login;